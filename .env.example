# 环境变量配置示例

# HuggingFace API Key（可选）
# 从 https://huggingface.co/settings/tokens 获取
# 如果不配置，系统会自动使用关键词匹配
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# 注意：
# 1. 在GitHub中，这个key应该配置在 Settings → Secrets → Actions
# 2. 本地测试时，可以复制这个文件为 .env，并填入真实的key
# 3. .env 文件已被 .gitignore 忽略，不会被提交到GitHub
